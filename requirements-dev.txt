# Development and test tooling.
#
# NOT installed into the runtime image — Dockerfile installs requirements.txt
# only, so pytest, ruff, mypy and the fixture libraries no longer ship to
# production. Install locally or in CI with:
#
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Note: `bleach` deliberately lives in requirements.txt, not here, despite
# having been filed under "Testing & Development" — it is a hard runtime import
# in app/core/sanitize.py.

# The linters were pinned three years behind the runtime: ruff 0.1.8 does not
# recognise target-version "py313" and refuses to start, and its config lived
# under [tool.ruff] rather than [tool.ruff.lint]. They had never been run, so
# nobody noticed. Bumped to versions that understand Python 3.13.
-r requirements.txt

black==26.5.1
factory-boy==3.3.0
faker==21.0.0
mypy==2.3.0
pytest-asyncio==0.23.8
pytest-cov==4.1.0
pytest-env==1.1.1
pytest-mock==3.12.0
pytest==8.0.0
ruff==0.16.0
